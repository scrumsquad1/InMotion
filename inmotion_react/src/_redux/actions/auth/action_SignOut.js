export const TYPE_AUTH_SIGNOUT_START = 'type_auth_signout_start';
export const TYPE_AUTH_SIGNOUT_COMPLETE = 'type_auth_signout_complete';
export const TYPE_AUTH_SIGNOUT_ERROR = 'type_auth_signout_error';

export default () => (dispatch) => {

    // this.dispatchStart(dispatch);
    // fetcher.clearAuthentication();
    // this.dispatchComplete(dispatch);
    
}